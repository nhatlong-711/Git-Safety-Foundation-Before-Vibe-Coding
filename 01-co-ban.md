# Phần 1: Các Lệnh Git Cơ Bản

Để bắt đầu với Git, bạn cần nhớ 4 lệnh cốt lõi sau:
* `git init`: Khởi tạo một repo mới.
* `git status`: Kiểm tra trạng thái các file.
* `git add <tên-file>`: Đưa file vào khu vực Staging Area.
* `git commit -m "tin-nhan"`: Lưu lại ảnh chụp (snapshot) của mã nguồn.